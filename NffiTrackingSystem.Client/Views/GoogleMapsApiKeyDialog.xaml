<Window x:Class="NffiTrackingSystem.Client.Views.GoogleMapsApiKeyDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Google Maps API Key" Height="280" Width="580" WindowStartupLocation="CenterOwner" ResizeMode="NoResize">
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/><RowDefinition Height="*"/><RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" TextWrapping="Wrap" Margin="0,0,0,10"
                   Text="Enter your Google Maps API key to use Google Maps. Or click 'OpenStreetMap (fara cheie)' to use a keyless map alternative."/>
        <TextBlock Grid.Row="1" Margin="0,0,0,4" FontWeight="Bold" Text="Google Maps API key:"/>
        <TextBox Grid.Row="2" x:Name="ApiKeyTextBox" Height="28" Margin="0,0,0,8"
                 VerticalContentAlignment="Center" FontFamily="Consolas" Text="YOUR_GOOGLE_MAPS_API_KEY"/>
        <TextBlock Grid.Row="3" TextWrapping="Wrap" FontSize="11" Foreground="Gray"
                   Text="The key usually starts with 'AIza' and can be obtained from console.cloud.google.com. Without a valid key, OpenStreetMap (keyless) will be used."/>
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button x:Name="SaveButton" Content="Save and use Google Maps" Width="220" Height="32" Margin="0,0,10,0" Click="SaveButton_Click"/>
            <Button x:Name="OfflineButton" Content="OpenStreetMap (fara cheie)" Width="220" Height="32" Margin="0,0,10,0" Click="OfflineButton_Click"/>
            <Button x:Name="CancelButton" Content="Cancel" Width="90" Height="32" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>